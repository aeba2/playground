#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../http-server-ssl/bin/http-server-ssl" "$@"
  ret=$?
else 
  node  "$basedir/../http-server-ssl/bin/http-server-ssl" "$@"
  ret=$?
fi
exit $ret
